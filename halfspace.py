"""
Computes halfspace projections of 3D bounding boxes
in spatial DB
Returns QSR based on halfspace projections
"""


def compute_projections(relation_graph):
    # Compute halfspace 3D projections for all
    # 3D geometries in given JSON representation

    return relation_graph

def add_directional_qsr(relation_graph):
    relation_graph = compute_projections(relation_graph)
    return relation_graph